m = float(input('Digite um valor em metros para ser convertido: '))
cm = m * 100
mm = m * 1000
print(f'O comprimento de {m} metros é equivalente a {cm:.2f} centímetros e {mm:.2f} milímetros.')

    # dm = m * 10
    # dam = m / 10
    # hm = m / 100
    # km = m / 1000
    # print(f'O comprimento de {m} metros é equivalente a {dm:.2f} decímetros, {dam:.2f} decâmetros, {hm:.2f} hectômetros e {km:.2f} quilômetros.')
